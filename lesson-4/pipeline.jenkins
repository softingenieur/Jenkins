pipeline {
    agent any

    stages {
        stage('Docker version') {
            steps {
                sh 'echo $USER'
                sh 'docker version'
            }
        }
        stage('Checkout') {
            steps{
                git branch: 'main',
                    url: 'https://github.com/softingenieur/Jenkins/tree/master/lesson-4.git'   

                 sh 'pwd'
                 sh 'ls -la'        
                }
        }
    }
}